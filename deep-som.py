# Load packages
import tensorflow as tf
import numpy as np
from numpy.linalg import eig
import pandas as pd
from minisom import MiniSom  
import math
from sklearn.metrics import classification_report


# Import data 
(x_train, y_train), (x_test, y_test) = tf.keras.datasets.mnist.load_data()

# Create kernal filter if you want it
# identity_kernel = np.array([[0,0,0],[0,1,0],[0,0,0]])
# identity_kernel.shape

# get sampled matrix for image using specific kernal size
kernel_size = (10,10)
sampled_matrix = get_sub_matrices(x_train[1], kernel_size)


  mmk0p;
 \
 \


 ]='[;[p;.lo9kim ,u7hnbytgv fr4cd esxwaz	Q		AQZSW2DECFD32WAQ	q`1alop90;
 -.≤'